// eslint.config.js
const eslintConfig = require('@electron-toolkit/eslint-config')

module.exports = [
  eslintConfig,
  {
    // Additional custom rules
    rules: {
      // Add any custom rules here
    }
  }
] 